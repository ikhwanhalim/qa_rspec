!!!
%html{:lang => "en"}
  %head
    %meta{:charset => "utf-8"}/
    %meta{:content => "IE=Edge,chrome=1", "http-equiv" => "X-UA-Compatible"}/
    %meta{:content => "width=device-width, initial-scale=1.0", :name => "viewport"}/
    %title= content_for?(:title) ? yield(:title) : "repair"
    = csrf_meta_tags
    / Le HTML5 shim, for IE6-8 support of HTML elements
    /[if lt IE 9]
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    = javascript_include_tag "flatly"
    = stylesheet_link_tag "flatly", :media => "all"
    = yield(:page_stylesheet) if content_for?(:page_stylesheet)
  %body
    / Fixed navbar
    .navbar.navbar-inverse.navbar-fixed-top
      %a.navbar-brand{:href => root_path}= image_tag("onapp.png", :class => "brand")
      .container
        .navbar-header
          %button.navbar-toggle{"data-target" => ".navbar-collapse", "data-toggle" => "collapse", :type => "button"}
            %span.icon-bar
            %span.icon-bar
            %span.icon-bar
          / %a.navbar-brand{:href => "#"} Integration Testing
        .navbar-collapse.collapse
          %ul.nav.navbar-nav
            %li
              %a{:href => root_path} Runs
            %li
              %a{:href => new_run_path} New run
            / %li.dropdown
            /   %a.dropdown-toggle{"data-toggle" => "dropdown", :href => "#"}
            /     Dropdown
            /     %b.caret
            /   %ul.dropdown-menu
            /     %li
            /       %a{:href => "#"} Action
            /     %li
            /       %a{:href => "#"} Something else here
            /     %li.divider
            /     %li.dropdown-header Nav header
            /     %li
            /       %a{:href => "#"} Separated link
            - if user_signed_in?
              %li= link_to "Sign out", destroy_user_session_path, :method => :delete
            - else
              %li= link_to "Sign in", new_user_session_path
    .container.top150
      .span12
        = render 'layouts/messages'
        = yield
    /
      Javascripts
      \==================================================
    / Placed at the end of the document so the pages load faster
 
    = yield(:page_javascript) if content_for?(:page_javascript)

